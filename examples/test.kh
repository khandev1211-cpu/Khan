import "postman"

let pm = pm_new_with_base("https://api.example.com")

pm = pm_get(pm, "health check", "/health")
pm = pm_post_json(pm, "create user", "/users", {"name": "Irfan"})
pm = pm_assert(pm, "2+2=4", 2 + 2 == 4)

pm_run(pm)